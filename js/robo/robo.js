/* =========================================================
   ГУРТОК РОБОТОТЕХНІКИ — спільний простір імен
   ---------------------------------------------------------
   Матеріали гуртка — це не покрокові інструменти, а довгі
   документи (КТП, поурочні плани, правила, схеми). Тому вони
   живуть поза реєстром інструментів: розділ = запис у
   ROBO.sections, який показує js/ui/doc.js.

   Розділи наповнюють sections.js, mod1.js, mod2.js —
   кожен запис це {title, html}.
   ========================================================= */
const ROBO = {
  sections: {},   // id розділу -> {title, html}
  simGen: 0,      // покоління rAF-циклу демонстрації кола
  ktp: {},        // заповнені поля КТП — у пам'яті, як і решта даних
};
